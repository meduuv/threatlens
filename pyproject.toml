[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "threatlens"
version = "0.1.0"
description = "Offline defensive threat-finding summaries"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["src"]
